============
Installation
============

At the command line::

    $ easy_install django-rolodex

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-rolodex
    $ pip install django-rolodex
